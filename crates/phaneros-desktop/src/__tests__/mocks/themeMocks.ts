import { ThemeState } from '@/types';

export const mockLightTheme: ThemeState = {
  theme: 'light',
  toggleTheme: () => {},
  setTheme: () => {},
};

export const mockDarkTheme: ThemeState = {
  theme: 'dark',
  toggleTheme: () => {},
  setTheme: () => {},
};
